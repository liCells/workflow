<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.lz.workflow.mapper.FlowMapper">
    <insert id="insertToRunning">
        insert into flow_running (id, name, symbol, start_time, executor)
        values (#{id}, #{name}, #{symbol}, #{startTime}, #{executor})
    </insert>

    <insert id="insertToHistory">
        insert into flow_history (id, name, symbol, start_time, executor, state)
        values (#{id}, #{name}, #{symbol}, #{startTime}, #{executor}, #{state.getName()})
    </insert>
</mapper>
